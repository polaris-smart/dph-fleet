#!/usr/bin/env sh
# fleet8 SSH 池 CLI 壳：指向预编译产物 dist（Node 22 下 node_modules 内 .ts 无法 type stripping，故发布带编译）。
BIN_DIR="$(python3 -c "import os,sys; print(os.path.dirname(os.path.realpath(sys.argv[1])))" "$0")"
exec node "$BIN_DIR/../dist/ssh/cli.js" "$@"
